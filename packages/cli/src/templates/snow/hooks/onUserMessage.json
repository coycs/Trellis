{
  "onUserMessage": [
    {
      "description": "Prepend Trellis breadcrumb on each user message (UI keeps original typed text)",
      "hooks": [
        {
          "type": "command",
          "command": "trellis hook workflow --platform snow",
          "timeout": 15000,
          "enabled": true
        }
      ]
    }
  ]
}
